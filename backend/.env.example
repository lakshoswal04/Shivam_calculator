# Development defaults. In Render these are set as service environment variables.
DATABASE_URL=postgresql://calcapp:calcapp_dev_pw@localhost:5432/legal_rules_dev
JWT_SECRET=dev-only-secret-not-for-production-use-32b+
CORS_ORIGINS=http://localhost:3000
ENVIRONMENT=development
POOL_MIN=1
POOL_MAX=10
